# Initial Setup

## 1. Enter configuration mode

```cli
root> configure
```

## 2. Set Default Password

```cli
root# set system root-authentication plain-text-password
root# new password: <password>
root# retype new password: <password>
root# commit
```
