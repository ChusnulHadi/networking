[Definition]

datepattern = %%Y-%%m-%%dT%%H:%%M:%%S(?:\.%%f)?(?:Z|[+-]\d{2}:\d{2})

failregex = ^.*sshd\[\d+\]: Failed password for (?:invalid user )?\S+ from <HOST> port \d+
            ^.*sshd\[\d+\]: Invalid user \S+ from <HOST> port \d+
            ^.*sshd\[\d+\]: Connection closed by (?:invalid user |authenticating user )?<HOST> port \d+ \[preauth\]

ignoreregex =
