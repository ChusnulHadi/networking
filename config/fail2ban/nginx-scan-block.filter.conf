[Definition]
# Match request yang return status error — 302 dihapus karena itu redirect normal
failregex = ^<HOST> - \S+ \[.*?\] "\S+ \S+ HTTP/\d\.\d" (400|401|403|404|405|408) \d+ ".*?" ".*?" ".*?"$

ignoreregex =
